The documentation for the C2 is included within the ``winc2`` directory. The rest of the scripts are pretty straightforward. ``domads.ps1`` adds 200 domain administrators, ``getsys.exe`` starts the ``Client.exe`` implant from the winc2 with SYSTEM level access, ``uacbyp.exe`` runs ``getsys.exe`` as administrator using UAC bypass, and nuke-ad.ps1 is a powershell script that will likely not be used because it would damage AD badly.
