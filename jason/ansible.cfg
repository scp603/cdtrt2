[defaults]
inventory = inventory.ini
host_key_checking = False
forks = 10               
timeout = 10
log_path = ./ansible.log

[privilege_escalation]
become = True
become_method = sudo
become_user = root