[defaults]
inventory = inventory.ini
host_key_checking = False      ; important for CTF, skips SSH fingerprint prompts
forks = 10                     ; run against N hosts in parallel
timeout = 10
log_path = ./ansible.log

[privilege_escalation]
become = True
become_method = sudo
become_user = root